#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

struct noabin
{
    char c;
    struc s noabin *esq, *dir;
};

int main()
{
    struct s_noabin *raiz;
    assert(raiz = malloc(sizeof(struct s_noabin)));
    raiz->c = 'A';
}